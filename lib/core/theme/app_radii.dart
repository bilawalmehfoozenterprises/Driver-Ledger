import 'package:flutter/material.dart';

/// App border radius constants for Driver Ledger
class AppRadii {
  AppRadii._();

  static const double field = 8;

  static const BorderRadius fieldRadius = BorderRadius.all(
    Radius.circular(field),
  );
}
